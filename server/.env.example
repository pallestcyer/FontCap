# ===========================================
# SUPABASE CONFIGURATION (Required)
# ===========================================
# Get these from your Supabase project dashboard:
# https://app.supabase.com/project/_/settings/api

SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key-here
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# ===========================================
# CLOUDFLARE R2 STORAGE (Required)
# ===========================================
# Font files are stored in Cloudflare R2 for better performance and no egress fees.
# R2 has a generous free tier: 10GB storage, 10M Class A ops, 1M Class B ops/month.
#
# Setup steps:
# 1. Go to Cloudflare Dashboard > R2 Object Storage
# 2. Purchase R2 (free tier available, no credit card required for free tier)
# 3. Create a bucket (e.g., "fontcap-fonts")
# 4. Go to Overview > Manage R2 API Tokens
# 5. Create API token:
#    - Choose "Create User API token" (or Account API token if you're Super Admin)
#    - Set Permission to "Object Read & Write"
#    - Optionally scope to your bucket for extra security
# 6. Copy the Access Key ID and Secret Access Key
#
# Your Account ID is in the R2 dashboard URL: dash.cloudflare.com/<ACCOUNT_ID>/r2

R2_ACCOUNT_ID=your-cloudflare-account-id
R2_ACCESS_KEY_ID=your-r2-access-key-id
R2_SECRET_ACCESS_KEY=your-r2-secret-access-key
R2_BUCKET_NAME=fontcap-fonts

# ===========================================
# JWT CONFIGURATION (Required in Production)
# ===========================================
# Generate secure secrets with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
# CRITICAL: These MUST be set in production - server will fail to start without them

JWT_SECRET=CHANGE_THIS_TO_A_SECURE_RANDOM_STRING_IN_PRODUCTION
JWT_REFRESH_SECRET=CHANGE_THIS_TO_ANOTHER_SECURE_RANDOM_STRING

# ===========================================
# SERVER CONFIGURATION
# ===========================================

NODE_ENV=development
PORT=3000

# CORS Configuration (comma-separated allowed origins)
# In production, set this to your Electron app's origin or your web domain
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000,http://127.0.0.1:5173

# ===========================================
# OPTIONAL CONFIGURATION
# ===========================================

# File Upload Limits (bytes) - default: 10MB
MAX_FILE_SIZE=10485760

# Optional: Email configuration for notifications (future feature)
# SMTP_HOST=
# SMTP_PORT=
# SMTP_USER=
# SMTP_PASS=
